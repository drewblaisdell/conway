define([], function() {
  return {
    // render settings
    aliveCellColor: '#00aaff',
    deadCellColor: '#ffffff',
    cellSize: 8,
    cellSpacing: 1,

    // grid settings
    gridWidth: 100,
    gridHeight: 70,

    // game settings
    generationDuration: 500
  };
});